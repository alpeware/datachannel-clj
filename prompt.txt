Act as a Principal Software Engineer. Your objective is to execute Phase 1 of the public API refactor defined in `DESIGN-API.md`. We are migrating the library to a strict Sans-IO architecture.

PHASE 1 SCOPE (CORE & TESTS ONLY):
1. Update `datachannel.core` to expose the pure `handle-receive` and `handle-timeout` function signatures exactly as defined in `DESIGN-API.md`.
2. Ensure these core functions return the `{:new-state ..., :network-out [...], :app-events [...]}` map.
3. Remove all usage of callback atoms (e.g., `:on-open`, `:on-message`, `:on-data`, `:on-error`) from the connection state map and internal core logic. Emit these as pure data in the `:app-events` vector instead.
4. Refactor ALL existing test files (e.g., `handshake_test.clj`, `sctp_robustness_test.clj`, `sctp-init-abort-test.clj`) to use the new pure API. The tests must capture the returned map and assert against the `:app-events` vector, rather than relying on injected callback atoms.

AUTONOMY DIRECTIVE: 
- Do NOT pause to ask for human clarification, feedback, or permission. 
- You are an autonomous Principal Engineer.
- If you face ambiguity during the refactor, make a reasonable, isolated engineering decision independently and proceed.
- Do NOT implement the `datachannel.nio` namespace yet. That is Phase 2. Focus entirely on migrating the core logic and making the existing test suite pass with
the new API.