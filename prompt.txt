Act as an autonomous Principal Software Engineer operating in a continuous CI loop. The architectural foundation for `datachannel-clj` (Sans-IO, MTU Packetization, Stream Multiplexing, Inbound Reassembly, Congestion Control, Flattened State Map, and Unified Timers) is now COMPLETE.

Do NOT initiate any major architectural refactors of the state machine without explicit permission. Your goal is strictly test-driven feature completion.

PRIORITY 1: BURN DOWN TESTING.MD
Resume implementing the missing test cases from `TESTING.md`.
1. Pick ONE unchecked test.
2. CRITICAL: Remember that we recently flattened the `create-connection` state (removed the `atom` entirely) and unified the timer routing under a `defmulti handle-timeout-timer`. You must write your new test assertions and setup logic to match this pure state map.
3. Implement any missing minor functionality required for that test (e.g., specific flag handling, edge-case timers).
4. Write the test, ensure the CI is green, and add it to the runner.

CRITICAL WORKFLOW DIRECTIVE: SMALL PRs
- Work in small, isolated PRs.
- Maintain strict functional purity.
- Make independent engineering decisions and submit your PRs.