Act as an autonomous Principal Software Engineer operating in a continuous CI loop. We just completed a major architectural refactor adding the MTU `packetize` pipeline and `:streams` multiplexing.

PRIORITY 1: RESTORE DISABLED TESTS
Several tests were temporarily commented out in `test_runner.clj` during the architecture refactor.
- You must go into `test_runner.clj`, uncomment ONE of the disabled tests, update the test code to match the new `:streams` and `packetize` state map architecture, and get it passing.
- Do not attempt to fix all disabled tests in one PR. Fix them incrementally.

PRIORITY 2: TEST-DRIVEN DEVELOPMENT
Once all tests in `test_runner.clj` are restored and passing, resume implementing missing test cases from `TESTING.md`.
1. Pick ONE unchecked test (Prioritize the Stream Multiplexing and PR-SCTP tests).
2. Implement any missing core functionality required for that test.
3. Write the test, ensure it passes, and add it to the runner.

CRITICAL WORKFLOW DIRECTIVE: SMALL PRs
- Work in small, isolated PRs.
- Maintain strict functional purity in `datachannel.core`.
- Make independent engineering decisions and submit your PRs without asking
 for permission.
